import num

def maxelement(arr):

	
	r = len(arr)
	c = len(arr[0])

	for i in range(r):

		max1 = 0
		for j in range(c):
			if arr[i][j] > max1:
				max1 = arr[i][j]

		
		print(max1)

arr = [[3, 4, 1, 8],
